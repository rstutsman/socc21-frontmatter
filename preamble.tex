\documentclass[11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage{multicol}
\usepackage{microtype}
\usepackage{tabularx}
% The final version submitted had MyriadPro as the font
% to match the rest of the material generated by HotCRP
% Overleaf does not support MyriadPro, so it was compiled
% locally. For compiling on Overleaf, use libertine as
% an alternative font.
%Alternatively, it should be possible to compile using
%XeLaTeX, and use ttf fonts

%\usepackage{MyriadPro}
\usepackage{libertine}

\usepackage[letterpaper, margin=1in]{geometry}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{color}
\usepackage{graphicx}
\usepackage{indentfirst}

\pagestyle{empty}
